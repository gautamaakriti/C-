#include <iostream> 
using namespace std;
double computeSeries(int n){
    double sum = 0;
    for(double n = 1; n<= 1; n++){
        double series = / (i+1);
        sum = sum + series;
    }
    return sum;
}
int main (){
    int i;
    double sum;
    cout<<"Enter the compute series value: "<<endl;
    cin>>i;
    sum = computeSeries(i);
    cout<<"The sum of seies: "<<sum<<endl;
    return 0;
}